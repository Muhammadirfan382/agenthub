# Development and CI dependencies (pinned). Justification:
#   pytest    test runner
#   httpx2    HTTP client required by FastAPI/Starlette TestClient
#   aiosqlite async SQLite driver: local development and fast tests without Docker
#   ruff      linting (incl. flake8-bandit security rules) and formatting
#   mypy      static type checking
-r requirements.txt
aiosqlite==0.22.1
httpx2==2.13.0
mypy==2.3.1
pytest==9.1.1
ruff==0.16.7
